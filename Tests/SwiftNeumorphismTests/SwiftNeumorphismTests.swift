    import XCTest
    @testable import SwiftNeumorphism

    final class SwiftNeumorphismTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftNeumorphism().text, "Hello, World!")
        }
    }
